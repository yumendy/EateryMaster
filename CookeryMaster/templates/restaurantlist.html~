{% extends "userpanel.html" %}

{% block title %}All Restaurant{% endblock %}

{% block content %}

<div class="row">
	<h1 class="sub-header text-center">All Restaurant</h1>
</div>
<div class="row col-sm-10 col-sm-offset-1">
	<div class="well">
		{% for s in school_list %}
			<ul class="list-group">
				<h4 class="list-group-item-heading text-cneter">{{ s.name }}</h4>
				{% for r in s.restaurant_set.all %}
					<a href="/canteens/?id={{ r.id }}" class="list-group-item">{{ r.name }}</a>
				{% empty %}
					<li class="list-group-item">Have no restaurants!</li>
				{% endfor %}
			</ul>
		{% empty %}
			<h4 class="text-center">Have no restaurants!</h4>
		{% endfor %}
	</div>
</div>

{% endblock %}