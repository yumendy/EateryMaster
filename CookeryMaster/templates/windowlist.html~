{% extends "userpanel.html" %}

{% block title %}All Windows{% endblock %}

{% block content %}

<div class="row">
	<h1 class="sub-header text-center">All Windows</h1>
</div>
<div class="row col-sm-10 col-sm-offset-1">
	<div class="well">
		{% for r in restaurant_list %}
			<ul class="list-group">
				<h4 class="list-group-item-heading text-cneter">{{ r.name }}</h4>
				{% for w in r.window_set.all %}
					<a href="/windows/?id={{ w.id }}" class="list-group-item">{{ w.name }}</a>
				{% empty %}
					<li class="list-group-item">Have no windows!</li>
				{% endfor %}
			</ul>
		{% empty %}
			<h4 class="text-center">Have no windows!</h4>
		{% endfor %}
	</div>
</div>

{% endblock %}