{% extends "userpanel.html" %}

{% block title %}overview{% endblock %}

{% block content %}

<div class="row" id="id_for_index_overview">
	<h1 class="sub-header text-center">{{ username }}</h1>
</div>
<div class="row col-sm-10 col-sm-offset-1" id="id_for_index_allass">
	<div class="well">
		<div class="list-group">
			<h3 class="list-group-item-heading text-center">All Assessment</h3>
			{% for a in assessment_list %}
			<a href="/dishes/?id={{ a.dish.id }}#id_for_index_{{ a.id }}" class="list-group-item">
				<h4 class="list-group-item-heading">{{ a.dish.window.restaurant.name }}->{{ a.dish.window.name }}->{{ a.dish.name }}</h4>
				<p class="list-group-item-text">{{ a.content }}</p>
			</a>
			{% empty %}
				<li class="list-group-item">Has no assessment</li>
			{% endfor %}
		</div>
	</div>
</div>
<div class="row col-sm-10 col-sm-offset-1" id="id_for_index_allmessage">
	<div class="well">
		<div class="list-group">
			<h3 class="list-group-item-heading text-center">All Message</h3>
			{% for m in message_list %}
			<a href="/guestbook/?#id_for_index_{{ m.id }}" class="list-group-item">
				<h4 class="list-group-item-heading">{{ m.title }}</h4>
				<p class="list-group-item-text">{{ m.content }}</p>
			</a>
			{% empty %}
				<li class="list-group-item">Has no message</li>
			{% endfor %}
		</div>
	</div>
</div>
{% for c in category_list %}
<div class="row col-sm-10 col-sm-offset-1" id="id_for_index_category_{{ c.id }}">
	<div class="well">
		<div class="list-group">
			<h3 class="list-group-item-heading text-center">All {{ c.name }}</h3>
			{% for item in c.news_set.all %}
			<a href="" class="list-group-item">
				<h4 class="list-group-item-heading">{{ item.title }}</h4>
				<p class="list-group-item-text">{{ item.content }}</p>
			</a>
			{% empty %}
				<li class="list-group-item">Has no news</li>
			{% endfor %}
		</div>
	</div>
</div>
{% endfor %}

{% endblock %}